id: glassy
version: "1"
name: Glassy
description: Entry mode — reads the spec and delivers the artifact its task asks for; one cheap stage, the oracle lives in the task spec.
stages:
  - role: run
    model_ref: tier:cheap
    oracle: true
    max_attempts: 3
on_fail: halt
