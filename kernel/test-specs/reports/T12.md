# T12 — CI for the kernel

## Verdict: FAIL

The two T12-specific oracle conditions hold: the workflow file validates
(pyyaml structural check; `actionlint` unavailable on this machine), and
the deliberate red — a temporary bad vector on disposable scratch branch
`scratch/t12-deliberate-red` — fails the job's test step (exit 101,
attributed by name). The spec artifact exists: `.github/workflows/kernel.yml`
plus the one line in `bin/check-saude.sh`.

Verdict is FAIL for the same reason T11 is: the every-spec mechanical
oracle — `cd kernel && cargo test --release` exits 0 on the branch — is
**deterministically red** on `p1-T12`. That red is not T12's doing and not
lost work: T12 touches no `kernel/dispatch-policy/src/**` and no test
logic. It is the L3 stable-sort tie, which this branch now pins as its own
promoted evidence (finding 1): running the CI's own laws gate
(`PROPTEST_CASES=2000`) found a third counterexample, proptest
auto-persisted it, and every subsequent `cargo test --release` replays it.
Resolving this is the owner decision already pending since T02/T08/T10
(scope the determinism clause or tie-break the sort). Per battery rules
the gates ship red and documented, not weakened.

## Evidence

```text
$ cd /tmp/p1-T12 && git worktree add /tmp/p1-T12 -b p1-T12 main   # HEAD 1425b9c
# spec source: kernel/test-specs/T11-T20-second-wave.md — referenced by the
# committed README but UNTRACKED in git; read (read-only) from the owner's
# tree and committed verbatim into this branch (finding 4).

$ cd kernel && cargo fmt --check | grep -c '^Diff in'
38        # src/lib.rs 21, src/main.rs 4, tests/laws.rs 6, tests/vectors.rs 7
$ cargo clippy --all-targets -- -D warnings
error: could not compile `dispatch-policy` (test "vectors") … exit 101
  = help: clippy::derivable_impls … tests/vectors.rs:35 (enum Override)

$ cargo test --release                      # fresh tree, before any 2000-case run
test result: ok. … 6 passed (laws incl. l3_cheap_order), 1 passed (vectors)  exit 0

$ PROPTEST_CASES=2000 cargo test --release --test laws
failures: l3_cheap_order … successes: 604 … exit 101
$ cat tests/laws.proptest-regressions
cc bdced3f9a13effc5c4fa930dfdb7785b4fa43918… # shrinks to cat = [ref "/", provider None, keyless true, …, ref "/", provider Some("opencode"), keyless true]
# ^ file auto-created by proptest, untracked on main; committed on this branch

$ for i in 1 2 3 4 5; do cargo test --release >/dev/null 2>&1; echo $?; done   # post-persistence
1 1 1 1 1     # 6/6 red counting the in-kernel rerun — replay of the pinned seed

$ python3 - <<PY   # actionlint: not installed; act: not installed → pyyaml oracle
assert d['name'] == 'kernel'; on['push']['paths'] == ['kernel/**', '.github/workflows/kernel.yml']
steps: checkout@v4; dtolnay/rust-toolchain@stable (rustfmt, clippy); Swatinem/rust-cache@v2;
  cargo fmt --check; cargo clippy --all-targets -- -D warnings;
  law-presence (T13 check-laws.sh when present — self-skips with a loud echo);
  cargo test --release; laws proptest env PROPTEST_CASES="2000"
YAML OK + all five spec gates present

$ bash -n bin/check-saude.sh   # ok; new line, exactly one:
[ -d kernel ] && checar "kernel: cargo test (P1)" bash -c 'cd kernel && cargo test --release'
# guard check: kernel absent (fresh dir) → SKIP; kernel present → rc=101 today (inherits L3)

# deliberate red (scratch branch scratch/t12-deliberate-red off p1-T12@3535adb):
$ python3: append case T12_deliberate_red_scratch (= case 0 with expect.ok.entries = [])
$ cargo test --release --test vectors
T12_deliberate_red_scratch: got Ok( … )   # vectors.rs:95 panic, names the case
test result: FAILED. 0 passed; 1 failed                   exit 101   ← job step red
$ cargo test --release --no-fail-fast   # l3_cheap_order FAILED + all_p1_vectors_conform FAILED
$ git worktree remove --force /tmp/p1-T12-red && git branch -D scratch/t12-deliberate-red
```

## Findings

1. **bug (L3)** — The T12 laws gate (`PROPTEST_CASES=2000`) is
   deterministically red on the current tree, and its first CI run makes
   the whole branch oracle deterministically red: proptest persists the
   counterexample to `tests/laws.proptest-regressions`, which every later
   default `cargo test --release` (README oracle and the new check-saude
   line included) replays. Observed: fresh tree green once, then 6/6 red
   after the 2000-case run (wave 1 measured ~20% red without a pinned
   seed). Owner decision pending: scope the determinism clause or
   tie-break the sort. Gates deliberately not weakened.
2. **bug (L3)** — Third counterexample instance of the same
   duplicate-ref stable-sort tie class: `ref: "/"`, keyless pair —
   distinct from the two seeds promoted by T02/T08/T10 (`ref: "t"`,
   `ref: "c"`, non-keyless). Any one of the three kills `l3_cheap_order`.
3. **bug** — The workflow's first two gates are born red on every
   `kernel/**` push: `cargo fmt --check` (38 hunks) and
   `cargo clippy -D warnings` (`derivable_impls`, tests/vectors.rs:35).
   Mechanical fixes (`cargo fmt`; one derive) are an owner call, not the
   battery's. Law touched: none directly — build honesty, L-adjacent.
4. **gap (docs)** — `kernel/test-specs/README.md` (committed) points to
   `T11-T20-second-wave.md`, which was never committed: a fresh clone
   cannot execute T12–T20 at all. This run only got the spec by reading
   the owner's untracked file.
5. **gap** — T12's fifth gate (law-presence check) belongs to T13 and
   does not exist yet; wired as an existence-guarded step that skips
   loudly (`check-laws.sh not on this branch yet (T13 pending)`), so the
   job stays attributable until T13 lands and the union dissolves the
   guard. Decision recorded in `kernel/laws/P1.md` Decisions.
6. **note** — `actionlint` and `act` are not installed on the dispatch
   machine, so the spec oracle ran as: pyyaml structural validation
   (workflow parses; all five gates present; paths filter correct) plus
   local replay of the job's exact commands for the deliberate red.
   Unpromoted by kind: environment fact about the dispatch machine, no
   kernel artifact can pin it — declared here per the T11 audit pattern.

## Promoted

| Finding | Artifact (branch `p1-T12`) |
|---|---|
| spec artifact (T12 itself) | `.github/workflows/kernel.yml` |
| spec artifact (T12 itself) | `bin/check-saude.sh` — one line, `[ -d kernel ]` guard |
| 1, 2 | `kernel/dispatch-policy/tests/laws.proptest-regressions` — 3rd L3 seed (`bdced3f9…`), committed |
| 1, 3 | `kernel/laws/P1.md` — "Not laws (yet)": fmt/clippy/L3-at-CI-volume bullets with the `kernel.yml` steps as mechanisms |
| 5 | `kernel/laws/P1.md` — new `## Decisions` table: 2000-case gate ships red; T13 self-skip guard; check-saude one-liner rationale |
| 4 | `kernel/test-specs/T11-T20-second-wave.md` — committed verbatim (111 lines; was untracked) |
| this run | `kernel/test-specs/reports/T12.md` (this file) |
| 6 | none (declared unpromoted) |

`git diff main..p1-T12 -- kernel/dispatch-policy/src` is empty: no kernel
source was touched. Branch committed, not merged.
