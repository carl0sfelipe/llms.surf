# T20 — The battery as a llms.surf mode

## Verdict: PASS

`core/modes/kernel_test.yaml` (schema v1) is a BMAD cheap/dev shape: stage
`run` on `tier:cheap`, then mechanical `command:` →
`kernel/test-specs/oracle.sh <ID>`. `oracfit run kernel_test` is multi-stage
(`dispatch-stages.sh`). Ledger records `provider_efetivo`, `attempt`,
`oracle_exit`. Honesty counts: 21 modes.

```
writes: core/modes/kernel_test.yaml, kernel/test-specs/oracle.sh, bin/dispatch.sh, bin/dispatch-mode.sh, bin/dispatch-stages.sh, bin/test-kernel-test-mode.sh, bin/check-saude.sh, bin/test-oracfit-mode-loader.sh, kernel/test-specs/T11-T20-second-wave.md, kernel/test-specs/reports/T20.md, README.md, site/app.js, site/llms.txt
reads: docs/research/bmad-tiers-2026-09-19.md, kernel/test-specs/README.md, bin/lib-oracfit-mode-loader.py
oracle: python3 bin/lib-oracfit-mode-loader.py validate core/modes/kernel_test.yaml && bash bin/test-kernel-test-mode.sh
```

## Evidence

```
$ python3 bin/lib-oracfit-mode-loader.py validate core/modes/kernel_test.yaml
OK core/modes/kernel_test.yaml roles=['run', 'run']
EXIT=0

$ bash bin/test-kernel-test-mode.sh
=== 5 passed, 0 failed ===
EXIT=0

$ bash bin/test-oracfit-mode-loader.sh
=== RESULTS: 15 passed, 0 failed ===
EXIT=0

$ bash bin/check-docs.sh && bash tests/test-site-honesty.sh
21 modes / 114 incidents / 37 suites
EXIT=0 EXIT=0

$ bash bin/check-saude.sh
✅ T20 kernel_test modo + ledger      ok
✅ Framework são
EXIT=0
```

`src/**` untouched.

## Promoted

- `core/modes/kernel_test.yaml`
- `kernel/test-specs/oracle.sh` (README rule 2 judge)
- `bin/test-kernel-test-mode.sh`
- `DISPATCH_EFETIVO_FILE` on `dispatch-mode.sh` and `dispatch-stages.sh`
- `bin/check-saude.sh` T20 hook
- honesty counts 20 → 21 modes

## Out of scope

- Cutting `LLMS_KERNEL` default to `on`
- Real-model dispatch (stubs only)
- `kernel/dispatch-policy/src/**`
