# T13 — Law presence gate (`kernel/scripts/check-laws.sh`)

## Verdict: PASS

The gate is built, demonstrably bites in both directions, and the branch
is green end-to-end: `./kernel/scripts/check-laws.sh` exit 0 and
`cd kernel && cargo test --release` exit 0. Caveat on the spec itself:
`kernel/test-specs/T13-*.md` does not exist in the repo (finding 1); the
run executed the spec inlined in the dispatch prompt, now written down as
`kernel/test-specs/T13-law-presence-gate.md` on this branch.

## Evidence

```text
$ cd /home/carlos/.zcode/workspace/default/llms.surf
$ git worktree add /tmp/p1-T13 -b p1-T13 main   # main @ 1425b9c
$ # spec search: no T13-*.md anywhere; README references T11-T20-second-wave.md — also absent
$ for b in main p1-wave1 p1-T12 p1-T01…T10; do git ls-tree -r --name-only $b | grep -E 'T1[1-9]|second-wave'; done
p1-wave1:kernel/test-specs/reports/T11.md        # only second-wave trace in the repo

$ ./kernel/scripts/check-laws.sh                 # pristine main, before promotions
FAIL: P1.md has no '## Decisions' heading
FAIL: law id L7 (P1.md) appears in neither kernel/dispatch-policy/tests/ nor kernel/vectors/
check-laws: RED
exit=1

$ # promotions: L7 tag in tests/laws.rs doc comment; '## Decisions' heading + table in P1.md
$ ./kernel/scripts/check-laws.sh
check-laws: green (1 file(s) scanned, ids present, Decisions headings in place)
exit=0

$ printf '…fake L99 row…\n' >> kernel/laws/P1.md   # oracle drill, temporary
$ ./kernel/scripts/check-laws.sh
FAIL: law id L99 (P1.md) appears in neither kernel/dispatch-policy/tests/ nor kernel/vectors/
check-laws: RED
exit=1
$ # fake reverted; git diff P1.md | grep -c L99 → 0 (never committed)

$ ./kernel/scripts/check-laws.sh && cd / && /tmp/p1-T13/kernel/scripts/check-laws.sh
check-laws: green … (twice; cwd-independent via $(dirname $0) root resolution)

$ cd kernel && cargo test --release
test result: ok. 6 passed; 0 failed … (laws.rs, incl. l3_cheap_order)
test result: ok. 1 passed; 0 failed … (vectors.rs)
ORACLE_EXIT=0
$ for i in 1…10; do cargo test --release --test laws; done → 10× "test result: ok"
```

## Findings

1. **gap (docs)** — the spec this run executed does not exist in the
   repository: no `kernel/test-specs/T13-*.md` on `main` or on any
   `p1-*` branch, and the second-wave index `T11-T20-second-wave.md`
   referenced by the battery README is likewise absent (only trace of the
   second wave is `reports/T11.md` on `p1-wave1`). The run followed the
   dispatch prompt's inline spec. Promoted: the spec written down
   (`T13-law-presence-gate.md`, with a provenance note), same branch.
2. **gap (L7)** — L7 ("gates filter but never reorder") was stated in
   `kernel/laws/P1.md` with mechanism `l2_l4_l5_l6_cheap_gates`
   (subsequence assertion), but the id `L7` appeared in neither
   `kernel/dispatch-policy/tests/` nor `kernel/vectors/`: the mechanism
   existed unnamed, and the presence gate went red on pristine main.
   Promoted: the subsequence bullet in `tests/laws.rs` now carries the
   `L7` tag (changed test, README rule 1). No behaviour change — comment
   only; 10/10 `cargo test --release --test laws` runs green after.
3. **gap (Decisions)** — `kernel/laws/P1.md` on main has no `## Decisions`
   heading (wave-1 branches added one; not merged to main), so the heading
   gate went red on pristine main. Promoted: `## Decisions` heading plus
   the wave-1-normalized table (`| Date | Behaviour | Why | Vector(s) |`)
   with one real decision row (finding 5's decision), same branch.
4. **note** — gate character: red on pristine main (both checks bite),
   green after promotions, red again on the fake `L99` drill (temporary,
   discarded, never committed), cwd-independent. Presence-only by design —
   a tagged-but-toothless mechanism stays green by construction; semantic
   honesty remains T06/law-review's job.
5. **note (decision recorded)** — law mechanisms are enforced
   presence-only via this script (like the AGENTS.md grep in bestmodel
   S25c); semantic strength stays with laws.rs/proptest, and CI wiring is
   deferred to P2 per P1.md's own "Rule for this directory". Recorded as a
   row in the new `## Decisions` table.
6. **note (oracle risk, pre-existing)** — the `l3_cheap_order` stable-sort
   flake documented by T02/T08/T10 did not reproduce in this run's sample
   (1× full suite + 10× laws suite, all green), but the L3 owner decision
   is still pending on main; until then the battery-wide oracle remains
   flake-exposed. Nothing fixed here (`src/` untouchable by battery rule).

## Promoted

- `kernel/scripts/check-laws.sh` — the spec's permanent artifact
  (executable, presence-only law-id gate + `## Decisions` heading gate).
- `kernel/laws/P1.md` — `## Decisions` heading + normalized table with the
  presence-only enforcement decision (finding 3/5).
- `kernel/dispatch-policy/tests/laws.rs` — `L7` tag on the subsequence
  assertion bullet of `l2_l4_l5_l6_cheap_gates` (finding 2).
- `kernel/test-specs/T13-law-presence-gate.md` — the executed spec written
  down, with provenance note (finding 1).
- `kernel/test-specs/reports/T13.md` — this report.
