# T18 — Shadow mode in the shell

## Verdict: PASS

`LLMS_KERNEL=shadow|on|off` in `bin/run-with-fallback.sh`. Shadow serves
the Python chain, diffs the credential-gated US wire against
`dispatch-policy`, writes `kernel_shadow_diff` to the ledger. D-SHELL:
kernel stderr is re-emitted; `id_status: null` no longer TypeErrors.

```
writes: bin/run-with-fallback.sh, bin/lib-free-credentials.sh, bin/ledger-finalize.sh, bin/test-free-path.sh, kernel/test-specs/T11-T20-second-wave.md, kernel/test-specs/reports/T18.md
reads: kernel/dispatch-policy, data/free-catalog.json, kernel/test-specs/DECISIONS-merge-inc3-t18.md
oracle: bin/test-free-path.sh → 0 (18/18)
```

## Evidence

```
$ bash bin/test-free-path.sh
=== RESULTADO: 18 pass, 0 fail ===
FREE:0
```

New legs:

- `id_status:null no registry não derruba o shell (D-SHELL)` — scratch
  registry with `"id_status": null`, stub runner, exit 0.
- `shadow dispatch: kernel_shadow_diff=0 no ledger` — `LLMS_KERNEL=shadow`
  `dispatch.sh tier:cheap` stub; ledger field is `0`.

Manual:

```
LLMS_KERNEL=shadow DISPATCH_EFETIVO_FILE=… run-with-fallback tier:cheap
# sidecar: kernel_shadow_diff=0
LLMS_KERNEL=on LLMS_KERNEL_BIN=/tmp/missing → exit 3 (no silent Python)
LLMS_KERNEL=off (default) → Python path unchanged
```

US comparison is after the same file-credential gate the shell loop
applies (kernel already gates via `--credentials "$(free_cred_providers_csv)"`).
Raw Python `resolve-tier` lists keyed openrouter refs that the kernel
skips when the file credential set is empty — comparing pre-gate Python
to kernel would always be diff=1 in CI.

`kernel/dispatch-policy/src/**` untouched.

## Findings

1. **gap (test isolation).** Legs 8–9 left `FREE_PROVIDER_ALLOWLIST` and
   `ORACFIT_AUTH_JSON` exported; shadow then compared kernel (tampered
   allowlist) to Python (no allowlist) → false diff=1. Promoted: unset
   before legs 12–13.

## Promoted

- `LLMS_KERNEL=shadow|on|off` in `bin/run-with-fallback.sh`
- `free_cred_providers_csv` in `bin/lib-free-credentials.sh`
- ledger field `kernel_shadow_diff` (additive, only when sidecar exists)
- `bin/test-free-path.sh` legs 12 (null id_status) and 13 (shadow diff=0)
- footprint block on T18 in `T11-T20-second-wave.md`

## Out of scope

- T19 `policy_version` (next PR)
- T20 mode YAML (next)
- Cutting over default to `on`
