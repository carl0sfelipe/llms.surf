# T11 — Promote the first wave

## Verdict: FAIL

Merges themselves are clean (9 branches, all conflicts resolved as unions,
no `src/` change), and every `## Promoted` claim of wave 1 exists on this
branch. The mechanical oracle is **red**: `cd kernel && cargo test
--release` exits 101 — `tests/laws.rs::l3_cheap_order` fails
deterministically (12 passed, 1 failed) because the regression seeds
promoted by T02/T08/T10 replay the stable-sort tie counterexample against
`src/lib.rs`. The red is the wave's own promoted evidence, not lost work:
`src/**` was never edited (battery rule), so the L3 resolution (scope the
determinism clause or tie-break the sort) is an **owner decision still
pending** — recorded in `kernel/laws/P1.md` "Not laws (yet)" (T02/T08/T10
bullets) with two pinned seeds in
`kernel/dispatch-policy/tests/laws.proptest-regressions`.

## Evidence

```text
$ git merge --no-ff p1-T01 …p1-T10   # 9 merges, T07 absent (rate-limited in wave 1; re-run pending)
# conflicts resolved as unions: cases.json (dedupe by name via jq-equivalent python),
# BENCH.md (rows), P1.md (Decisions rows normalized to | Date | Behaviour | Why | Vector(s) |,
# "Not laws (yet)" bullets, T09 "Statement amendments" section kept whole),
# tests/laws.rs (T04 cli_* block + T06 l4/l6 tests), laws.proptest-regressions (both seeds)

$ cd kernel && cargo test --release
…
failures:
    l3_cheap_order
test result: FAILED. 12 passed; 1 failed; … finished in 0.03s
error: test failed … exit 101

$ python3 cases.json check → 31 vectors, 0 duplicate names (13 original + 18 promoted)

## Promoted-path audit (README rule 1): 31/31 declared paths exist.
#   (T09's "kernel/laws/README" line is the *fixed dangling reference*,
#    not a path — P1.md now says so explicitly in its header.)
```

## Finding → artifact → spec (wave 1 consolidated)

| Spec | Finding (short) | Severity | Artifact promoted (in this branch) | Unpromoted |
|---|---|---|---|---|
| T01 | clippy `-D warnings` red (`derivable_impls`, tests/vectors.rs:41) | bug | `kernel/laws/P1.md` Not-laws candidate + mechanism (CI gate) | — |
| T01 | `cargo fmt --check` red (38 hunks / 4 files) | bug | `kernel/laws/P1.md` Not-laws candidate + mechanism (CI gate) | — |
| T01 | build numbers (6.6 s cold, offline byte-identical, 774,504 B, deps libc/libgcc_s only) | note | `kernel/BENCH.md` 3 rows | — |
| T02 | **L3 red at `PROPTEST_CASES=5000`; default suite red ~20% of runs** (stable-sort tie) | bug | vector `T02_l3_duplicate_ref_ties_keep_file_order`; seed in `laws.proptest-regressions`; P1.md Not-laws | owner decision (scope vs tie-break) — pending, not an artifact |
| T02 | L4: keyed entry, provider `null` skips credential gate; wire `provider ""` | gap | vector `T02_keyed_entry_unknown_provider_skips_credential_gate`; P1.md Decisions | — |
| T02 | `EmptyAfterCredentialGate` reached by only 2.2% of generated inputs (< 5% target) | gap | documented in `reports/T02.md` (distribution study) | no mechanism artifact (below spec's own 1% gap bar) |
| T02 | proptest `SourceParallel` regression-file byproduct | note | P1.md "Run byproduct" paragraph | — |
| T03 | duplicate refs gated **per occurrence**; shell collapses by ref (618/2250 mismatches, one class) | gap | 3 vectors `T03_*`; P1.md Decisions + Not-laws | — |
| T03 | dead-id gate precedes allowlist (order unpinned) | note | vector `T03_dead_id_gate_precedes_allowlist` | — |
| T04 | unknown `--format` silently renders US, exit 0 | note | P1.md Decisions D1 (T04); test `cli_unknown_format_value_falls_back_to_us` | — |
| T04 | vector schema cannot pin CLI-level contract | gap | 5 `cli_*` tests in `tests/laws.rs` (real binary + fixtures from cases.json) | — |
| T05 | `"models": null` = exit 3; absent = empty registry | gap | P1.md Decisions; vector `T05_empty_registry_cheap` | — |
| T05 | direct `""` → `MalformedField`, not `UnknownModel` | gap | P1.md Decisions; vector `T05_direct_empty_id_is_malformed_not_chain` | — |
| T05 | duplicate registry ids: last `id_status` wins | note | vector `T05_duplicate_id_last_status_wins` | — |
| T05 | catalog parse errors demoted to exit 2 (`CatalogMissing`), not 3 | gap | P1.md Decisions; pinned by `cheap_without_catalog_is_loud` | exit-code split raised for owner, no decision yet |
| T05 | **provider `""` divergence: Rust=violation, shell=no-provider** | gap | P1.md Decisions; vector `T05_empty_provider_is_allowlist_violation` | resolution decided later by T14 |
| T05 | duplicate catalog ref (different keyless): both legs enter chain | gap | P1.md Decisions + vector `T05_duplicate_ref_keeps_both_legs` | dedup law decided later by T16 |
| T05 | allowlist verbatim match; gate scope; L1b at CLI; nested cli_hints; typed doors | note | vectors + `tests/vectors.rs` allowlist override + report | — |
| T06 | **L6: both `Chain::new` guards were dead code** (mutation M12 survived) | bug (high) | test `l6_chain_new_rejects_empty` (post-promotion: 15/15 mutations killed) | — |
| T06 | L4: env fallback M9 survives (nothing observes env) | gap | test `l4_env_is_never_an_input` | mechanism for T17 |
| T06 | `DEAD_ID_STATUSES` coverage: vectors pinned only FANTASMA | gap | vector `T06_nao_statuses_are_dead` | — |
| T08 | **shell swallows E5-M2 WARN** (`capture_output=True`, re-emits only on failure) | bug | vector `T08_dead_keyless_leg_skipped`; P1.md Not-laws (WARN mechanism) | shell-side fix — no artifact (out of kernel battery scope; lands with T18 shadow) |
| T08 | L3 counterexample pre-existing on main (independent repro) | bug | seed in `laws.proptest-regressions`; P1.md Not-laws | same owner decision as T02 |
| T08 | `id_status: null` crashes shell Python (`TypeError`), kernel normalizes | note | P1.md Not-laws candidate ("id_status always string") | — |
| T08 | shell 102.2 ms vs binary 1.1 ms (~91×) | note | `kernel/BENCH.md` row | — |
| T09 | L1 accepts `provider: Some("")` (generator can't produce it) | gap | P1.md Statement amendments (L1) | normalization decided later by T14 |
| T09 | L4 unknown-provider keyed case (D3) | gap | P1.md Decisions D3 (T09) + vector `cheap_keyed_unknown_provider_passes_gate` | — |
| T09 | L5 dead-before-allowlist precedence | gap | P1.md Statement amendments (L5) | — |
| T09 | L2/L6 statement scope (dead-stamped entries on non-cheap routes) | gap | P1.md Statement amendments (L2, L6); Decisions D2 (T09) | — |
| T09 | kernel-stricter-than-Python allowlist (operator docs) | gap | P1.md Decisions D1 (T09) | — |
| T09 | env clause structural, not test-enforced; L3 addendum; CR round-trip refuted; dangling README ref | note | P1.md amendments L3/L4 + header fix | — |
| T09 | dead Python branches inventory (port ledger) | note | listed in `reports/T09.md` only | **unpromoted (declared "no action")** |
| T10 | L3 red on main @ 1425b9c (10/10 repro, docs-only consumer caught it) | bug | seed in `laws.proptest-regressions`; P1.md Not-laws | same owner decision as T02/T08 |
| T10 | cross-implementation contract undocumented (paid-tier chains, stamped defaults, skip order, precedence, `provider: null`) | gap | `docs/research/specs/P1-dispatch-policy-rust.md` §"Cross-implementation contract"; P1.md Not-laws ×2 | — |
| T10 | portability 13/13 with vectors alone | note | `reports/T10.md` (5 proposed vectors listed there, validated against binary) | the 5 proposed vectors not added to `cases.json` (would have duplicated T05/T09 pins) |

## Findings

1. **bug (L3)** — the wave-1 oracle cannot go green without an owner
   decision: `l3_cheap_order`'s property states a determinism the
   implementation (and the Python reference) never had for tied sort
   keys. Evidence above; pinned seeds replay it deterministically. Not
   fixed here — `src/` is untouchable by battery rule and the honest
   resolutions (scope the clause, or tie-break + typed unique-ref
   invariant) are decisions, not repairs.
2. **gap (wave 1 honesty metric)** — 31/31 promoted claims honored; 2
   findings declared artifact-less by their own reports (T02 F3, T09 F9)
   and 1 shell-side fix (T08 F1) has no kernel-battery artifact — all
   listed in the table's `Unpromoted` column. Three findings (T05/T09
   `provider ""`, T05 dedup, T02 L3 scope) are waiting on second-wave
   decisions T14/T16, by design.
3. **note** — merge-tooling note for future waves: independent agents
   cut from the same `main` will always collide on
   `cases.json`/`P1.md`/`BENCH.md`; unions dedupe by name / normalize
   Decisions rows to `| Date | Behaviour | Why | Vector(s) |`.

## Promoted

- `kernel/test-specs/reports/T11.md` — this report, on branch `p1-wave1`.
- The consolidated table above (T11's own artifact: the honesty metric
  the spec asks for).
- Branch `p1-wave1` itself: union of p1-T01, p1-T02, p1-T03, p1-T04,
  p1-T05, p1-T06, p1-T08, p1-T09, p1-T10 (no merge to `main`).
