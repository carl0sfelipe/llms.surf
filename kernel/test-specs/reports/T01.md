# T01 — Build & reproducibility

- Date: 2026-09-19
- Branch: `p1-T01` (worktree of llms.surf `main` @ `1425b9c`)
- Toolchain: rustc 1.98.1 (48a229cea 2026-09-01), cargo 1.98.1
- OS: Omarchy (Arch-based), Linux 7.1.9-arch1-2, x86_64, 28 cores, 32 GB RAM
- Budget: 20 min — spent ~12 min (no budget overrun)

## Verdict: FAIL

Steps 1, 4, 5, 6, 7 pass their oracles. Step 2 (`clippy -D warnings`) and
step 3 (`cargo fmt --check`) fail. Binary reproducibility, dependency
audit, offline build and binary facts are clean.

## Evidence

Step 1 — clean release build (PASS):

```
$ cd kernel && time (cargo build --release 2>&1 | tee /tmp/t01-build1.log)
    Finished `release` profile [optimized] target(s) in 6.64s
EXIT=0
$ grep -c warning /tmp/t01-build1.log
0
```

Step 2 — clippy (FAIL; clippy/rustfmt were not installed —
`rustup component add clippy rustfmt` succeeded first, so per spec this
is FAIL, not BLOCKED):

```
$ cargo clippy --release --all-targets -- -D warnings
error: this `impl` can be derived
  --> dispatch-policy/tests/vectors.rs:41:1
   |
41 | / impl Default for Override {
...
   = note: `-D clippy::derivable_impls` implied by `-D warnings`
error: could not compile `dispatch-policy` (test "vectors") due to 1 previous error
CLIPPY_EXIT=101
```

Step 3 — fmt (FAIL):

```
$ cargo fmt --check; echo "FMT_EXIT=$?"
Diff in /tmp/p1-T01/kernel/dispatch-policy/src/lib.rs:120: ...
FMT_EXIT=1
$ cargo fmt --check | grep -c '^Diff in'
38
$ cargo fmt --check | grep '^Diff in' | sed 's/:[0-9]*:$//' | sort | uniq -c
  21 .../dispatch-policy/src/lib.rs
   4 .../dispatch-policy/src/main.rs
   6 .../dispatch-policy/tests/laws.rs
   7 .../dispatch-policy/tests/vectors.rs
```

Step 4 — dependency audit (PASS): `cargo tree --edges normal` shows only
`serde` (→ serde_core, serde_derive → proc-macro2/quote/syn/unicode-ident)
and `serde_json` (→ itoa, memchr, zmij, serde_core); `grep -c proptest`
on the tree output = 0 (proptest is dev-dep only).

Step 5 — offline rebuild (PASS):

```
$ cargo clean && cargo build --release --offline
    Finished `release` profile [optimized] target(s) in 6.55s
OFFLINE_EXIT=0
```

Step 6 — reproducibility (PASS, no strip / --remap-path-prefix needed):

```
681be0812db027371a76a239e3dbfba5d7633de5951d38ada6656bbd00cf3e23  build 1 (online)
681be0812db027371a76a239e3dbfba5d7633de5951d38ada6656bbd00cf3e23  build 2 (--offline)
681be0812db027371a76a239e3dbfba5d7633de5951d38ada6656bbd00cf3e23  build 3 (online, after clean)
681be0812db027371a76a239e3dbfba5d7633de5951d38ada6656bbd00cf3e23  build 4 (online, after clean)
```

Step 7 — binary facts (PASS):

```
$ ls -l target/release/dispatch-policy
-rwxr-xr-x 2 carlos carlos 774504 Sep 19 12:45 target/release/dispatch-policy
$ ldd target/release/dispatch-policy
	linux-vdso.so.1
	libgcc_s.so.1 => /usr/lib/libgcc_s.so.1
	libc.so.6 => /usr/lib/libc.so.6
	/lib64/ld-linux-x86-64.so.2
```

No OpenSSL or other dynamic deps. `not stripped` (774 KB).

Branch oracle (README): `cd kernel && cargo test --release` → exit 0
(6 laws tests + `all_p1_vectors_conform` all pass on this branch).

## Findings

1. **bug** — `cargo clippy --release --all-targets -- -D warnings` exits
   101: `clippy::derivable_impls` at
   `kernel/dispatch-policy/tests/vectors.rs:41` (manual
   `impl Default for Override` returning `Override::Fixture`). Note the
   step-1 oracle passes only because `cargo build` does not compile test
   targets — the kernel is not lint-clean as a whole. Repro: `cd kernel
   && cargo clippy --release --all-targets -- -D warnings`. No P1 law
   touched (build hygiene).
2. **bug** — `cargo fmt --check` exits 1: 38 diff hunks across 4 files
   (`src/lib.rs` 21, `src/main.rs` 4, `tests/laws.rs` 6,
   `tests/vectors.rs` 7). Repro: `cd kernel && cargo fmt --check`. No P1
   law touched (build hygiene).
3. **note** — clippy and rustfmt components were absent from the default
   toolchain; `rustup component add` fixed both. Specs T02–T10 that lint
   or format must expect this cold-start cost.
4. **note** — everything else is clean and worth pinning: byte-identical
   sha256 across online/offline/repeat builds (no strip or
   `--remap-path-prefix` needed), lockfile complete for `--offline`,
   runtime deps exactly serde/serde_json closure with proptest confined
   to dev-deps, dynamic deps limited to libc/libgcc_s, cold build ≈ 6.6 s.

## Promoted

- `kernel/BENCH.md` (new) — 3 rows: cold release build 6.6 s / 0 warnings;
  offline rebuild exit 0; binary 774,504 B with stable sha256
  `681be081…cf3e23` across 3 builds and libc/libgcc-only dynamic deps.
- `kernel/laws/P1.md` → "Not laws (yet)" — 2 new candidate laws with
  proposed mechanisms (CI gates): tree rustfmt-clean; kernel clippy-clean
  under `-D warnings` (pins findings 1 and 2).
- No vectors promoted: T01 exercises the build, not policy outputs; no
  `cases.json` case applies and no dispatch behaviour was observed wrong.
