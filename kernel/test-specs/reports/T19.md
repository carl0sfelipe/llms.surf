# T19 — `policy_version` in the ledger

## Verdict: PASS

Additive object on the ledger line when `LLMS_KERNEL` is `shadow|on`:

```json
"policy_version": {"crate": "0.1.0", "kernel_sha": "<git rev-parse HEAD:kernel>"}
```

Absent when the flag is off. No existing ledger schema test in the tree;
the two `test-free-path.sh` assertions are the pin (not a weakening).

```
writes: bin/run-with-fallback.sh, bin/ledger-finalize.sh, bin/test-free-path.sh, kernel/test-specs/T11-T20-second-wave.md, kernel/test-specs/reports/T19.md
reads: kernel/dispatch-policy/Cargo.toml, kernel/
```

## Evidence

See `bin/test-free-path.sh` legs T19 (flag off / shadow). `src/**` untouched.

## Promoted

- sidecar keys `policy_version_crate` / `policy_version_sha`
- ledger field `policy_version` (object, additive)
- two free-path assertions

## Out of scope

T20 `kernel_test.yaml`.
