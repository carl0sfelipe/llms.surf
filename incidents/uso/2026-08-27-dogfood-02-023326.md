---
id: 2026-08-27-dogfood-02-023326
titulo: uso-dispatch mode/dogfood-02 → success
data: 2026-08-27
kind: uso-dispatch
source: mode
recorrivel: nao
regra: nao - telemetria automatica (incidents/2026-07-30-feedback-de-uso-forcado-por-humano.md)
status: uso-auto
task: dogfood-02
result: success
mode: 
harness: shell
---

# Uso automático: `mode` / `dogfood-02` → `success`

Gerado por `bin/emit-usage-feedback.sh` (incidents/2026-07-30-feedback-de-uso-forcado-por-humano.md). **Não** foi pedido por humano —
o harness coletou ao terminar o dispatch. Promova só se a evidência revelar padrão recorrente.

## Contexto

| Campo | Valor |
|---|---|
| ts (UTC) | 2026-08-27T05:33:26Z |
| source | `mode` |
| task | `dogfood-02` |
| result | `success` |
| mode | — |
| exit_code | 0 |
| harness | `shell` |
| host/user | `beelink` / `beelink` |
| workdir | `/home/beelink/Downloads/eaas-bitcoin` |
| git | `overnight/eaas-d3-header@03a419d` dirty=`sim` |
| spec | `/home/beelink/Downloads/eaas-bitcoin/specs/dogfood-02-transl-verifoffline-en.md` (5284 bytes, sha256=`b1ccc2c90f634dc72fb7d827e59f7cead8ebaf8a60656f0d55bbff7161ee1e68`) |
| oracle | `—` |
| latest_log | `—` |

## Env DISPATCH_* (sem secrets)

```
DISPATCH_MODEL_REF=vast3090flash/qwen38-flashnext-125b;DISPATCH_RUNNER=/home/beelink/Downloads/llms.surf/adapters/opencode/runner.sh;
```

## Ledger (linhas relevantes)

```

```

## Spec (head)

```
# Translar → EN: verificacao-offline (dogfooding llms.surf / endpoint 3090)

REGRAS DE OURO (violar qualquer uma = reprovação imediata):
- NUNCA use declare const ou qualquer símbolo fantasma como workaround — conteúdo real, direto no arquivo.
- Sua resposta deve ser o CONTEÚDO COMPLETO do arquivo traduzido — ou, se você é um agente com acesso a disco, escreva-o direto no caminho indicado.
- NÃO invente, arredonde nem "melhore" QUALQUER número, offset, tamanho em bytes, valor hexadecimal, identificador de API ou data.
- TODA estrutura de bytes/campos/códigos permanece EXATAMENTE como está: "EAAS", 0x45414153, 0x45414153 não muda; offsets 0..4/4..12/12..44/44..48; tamanhos 32B/1B/8B/4B/41 bytes; 2³²; ≥3 confirmações.
- Pseudocódigo e fórmulas viram inglês legível (function/root/sibling/left/right), mas a SEMÂNTICA criptográfica é intocável: SHA256(0x00 || leaf), SHA256(0x01 || left || right), k = maior potência de 2 menor que n vira "largest power of two LESS than n".
- NÃO remova links (mempool.space, blockstream.info), nomes de endpoints (/v1/proofs/{round_receipt}, /v1/proofs?studio_id=&game_id=&nonce=, /healthz) nem identificadores JSON (round_hash, leaf_index, tree_size, batch_id, h_root, merkle_path, beacon_round, btc_txid, anchor.status, annotations, failed_batches).
- Mantenha a estrutura: título H1, os 4 passos como seções H2, a tabela de estados de anchor.status (4 linhas: pending / awaiting_anchor / broadcast / confirmed), as seções Correções (AD-11) e Nota operacional.

## Dados verificados (copie SEM ALTERAR)
- "EAAS" = magic 0x45414153 (bytes 45 41 41 53); payload OP_RETURN de 48 bytes:
  offset 0..4 magic | 4..12 batch_id (u64, big-endian) | 12..44 merkle_root (32B) | 44..48 timestamp (u32, big-endian, Unix seconds UTC)
- leaf = round_hash (32B) || source (1B) || beacon_round (8B, big-endian) = exatamente 41 bytes; byte 33 = fonte (1 = primária, 2 = drand)
- receipt = "rr_" + hex(round_hash primeiros 12 bytes)
- codificação canônica v4 do preimage: entropy_val (32B), len+ASCII de studio_id/game_id, nonce (8B BE), source (1B), beacon_round (8B BE), recorded_at (4B BE Unix UTC), client_seed cru
- Merkle: RFC 6962 (Certificate Transparency), SHA-256 com separação de domínio 0x00 folha / 0x01 nó interno; path em ordem folha→raiz
- endpoints: GET /v1/proofs/{round_receipt} ; GET /v1/proofs?studio_id=&game_id=&nonce= ; /healthz status degraded
- fontes independentes citadas: mempool.space, blockstream.info, getrawtransaction
- estados: pending / awaiting_anchor / broadcast / confirmed (+ adiamento por taxa no awaiting_anchor)
- AD-11: resposta original imutável por construção; correções só em annotations com Lote posterior próprio; array hoje sempre vazio
- índice do operador é 100% reconstruível do disco; Lote que falha validação (CRC, raiz ou assinatura) sai do índice até restart

## ARQUIVO-ALVO: docs/en/verificacao-offline.md

## DELETE
(docs/en/verificacao-offline.md ainda não existe — este trabalho o cria.)

## REESCREVA
Produza \`docs/en/verificacao-offline.md\`: tradução profissional completa PT-BR → EN-US do
documento técnico "Offline Verification of an EaaS Round". Tom: documentação para
auditores/integradores — imperativo neutro, precisão cirúrgica nos invariantes.
Título H1 obrigatório: "Offline Verification of an EaaS Round". Seções na ordem:
intro (ferramentas necessárias: any SHA-256 implementation + an independent block
explorer), Step 1 (reproduce the round_hash — opcional para studios), Step 2
(reconstruct the Batch leaf), Step 3 (climb the Merkle path up to h_root),
Step 4 (check the Bitcoin anchor), Corrections (AD-11), anchor.status states
(table), Trust note, Operational note (for whoever runs the service).
```

## Log (tail do mais recente)

```
(sem log)
```

## Observações candidatas (julgamento humano / orquestrador)

- Run concluiu sem bloqueio — útil como baseline positivo do modo ?.
- Workdir dirty no fim — risco de misturar mudanças do modelo com estado prévio.
- Nenhum log da task em LOG_DIR — sensor cego parcial.

## Próximo passo

1. Se for ruído: ignore (já está `recorrivel: nao` — não gera dívida no audit).
2. Se revelar padrão: copie evidência para `bin/incident.sh new` no topo de `incidents/` e promova.
3. Telemetria máquina: `.dispatch/usage/usage.jsonl`

